      const requestBody = {
        type: 1,
        payload: {
          url: content,
        },
        designOptions: {
          backgroundColor: color,
          shape: 1,
        },
      };

      console.log("Backend'e gönderilen data:", requestBody);

      // Backend API çağrısı
      const response = await fetch(
        "https://qrgenerates.com/api/QRCode/generate",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            idToken: process.env.NEXT_PUBLIC_CLIENTID,
          },
          body: JSON.stringify(requestBody),
        }
      );